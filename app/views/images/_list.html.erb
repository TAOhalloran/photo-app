<table class="table table-striped">
  <thead>
    <tr>
      <th></th>
      <th></th>
      <th></th>
      <% if @user.id == current_user.id %>
        <th>Actions</th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @user_images.each do |user_image| %>
      <tr>
        <td><%= user_image.show %></td>
        <td><%= user_image %></td>
        <td><%= user_image %></td>
        <% if @user.id == current_user.id %>
          <td><%= link_to "Delete this Image", user_stock_path(user_stock), method: :delete,
            data: { confirm: "Are you sure?" }, class: "btn btn-xs btn-danger" %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
<table>